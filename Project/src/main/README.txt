Resources folder contains static languages such as html, css, and javascript. 
These files will form the frontend of your web application and will be responsible for rendering the user interface and handling user interactions.


Java folder contains backend. Java classes to implement the backend logic of your web application. These classes will handle business logic, data processing, and communicate with the frontend through HTTP requests and perform operations like search, reservation, cancellation, etc.